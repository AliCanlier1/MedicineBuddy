import 'package:flutter/material.dart';

class PMedicineHistory extends StatelessWidget {
  const PMedicineHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: const Center(child: Text("Medicine History")));
  }
}
