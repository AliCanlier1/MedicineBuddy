import 'package:flutter/material.dart';

class PMyProgress extends StatelessWidget {
  const PMyProgress({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("My Progress")),
    );
  }
}
